/** Node half of the browser details-tab plugin; its UI ships through `./client`. */

/** Host plugin body — the dashboard and feed are owned by host browser packages. */
export function apply(): void {}
